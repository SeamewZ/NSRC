The paired tests compare the deployed route with fixed DLinear: Celebrity MAE/MAPE, all News metrics, and all Macro-equities metrics remain significant after Holm correction, while Macro crypto's positive mean gains remain uncertain because all three CIs include zero. The 3- and 14-day block checks preserve this pattern. Table~\ref{tab:stability} shows positive MAE gains in both test halves for every cohort and gains across all horizons in three cohorts; Macro crypto retains gains at 17--20 of 24 horizons. The route loses in Celebrity's high-volatility bin, marking large shocks as a boundary for the current correction scheme.

Figure~\ref{fig:cases} shows origin-normalized 24-step paths; the flat macro outputs follow from the selected Persistence route.
